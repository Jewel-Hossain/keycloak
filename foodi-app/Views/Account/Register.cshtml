@model FoodiApp.Models.RegisterViewModel

@{
    ViewData["Title"] = "Register";
}

<div class="auth-container">
    <div class="auth-card">
        <div class="auth-header">
            <h1>Create Account 🎉</h1>
            <p>Join Foodi and get delicious food delivered!</p>
        </div>

        <form asp-action="Register" asp-controller="Account" method="post" class="auth-form">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            <div class="form-row">
                <div class="form-group">
                    <label asp-for="FirstName" class="form-label"></label>
                    <input asp-for="FirstName" class="form-control" placeholder="John" />
                    <span asp-validation-for="FirstName" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="LastName" class="form-label"></label>
                    <input asp-for="LastName" class="form-control" placeholder="Doe" />
                    <span asp-validation-for="LastName" class="text-danger"></span>
                </div>
            </div>

            <div class="form-group">
                <label asp-for="Username" class="form-label"></label>
                <input asp-for="Username" class="form-control" placeholder="johndoe" />
                <span asp-validation-for="Username" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Email" class="form-label"></label>
                <input asp-for="Email" class="form-control" type="email" placeholder="john@example.com" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>

            <!-- Foodi Internal Role -->
            <div class="role-section">
                <div class="role-section-header">
                    <h3>🎭 Foodi Role</h3>
                    <small>Controls your permissions within Foodi app</small>
                </div>
                <div class="form-group">
                    <label asp-for="SelectedRole" class="form-label"></label>
                    <select asp-for="SelectedRole" class="form-control">
                        @if (ViewBag.FoodiRoles != null)
                        {
                            @foreach (var role in ViewBag.FoodiRoles)
                            {
                                <option value="@role.Value">@role.Name - @role.Description</option>
                            }
                        }
                        else
                        {
                            <option value="Agent">Agent - Standard user with basic features</option>
                            <option value="Lead">Lead - Team management and advanced features</option>
                            <option value="Admin">Admin - Administrative features and reporting</option>
                            <option value="Head">Head - Full system access and user management</option>
                        }
                    </select>
                    <span asp-validation-for="SelectedRole" class="text-danger"></span>
                </div>
            </div>

            <!-- Keycloak Roles -->
            <div class="role-section">
                <div class="role-section-header">
                    <h3>🔐 Keycloak Roles</h3>
                    <small>Additional roles synced to Keycloak (optional, multi-select)</small>
                </div>
                <div class="form-group">
                    @if (ViewBag.KeycloakRoles != null && ViewBag.KeycloakRoles.Count > 0)
                    {
                        <div class="checkbox-group">
                            @foreach (var role in ViewBag.KeycloakRoles)
                            {
                                <div class="checkbox-item">
                                    <input type="checkbox" 
                                           name="SelectedKeycloakRoles" 
                                           value="@role.Value" 
                                           id="keycloak_@role.Value" />
                                    <label for="keycloak_@role.Value">
                                        <strong>@role.Name</strong> - @role.Description
                                    </label>
                                </div>
                            }
                        </div>
                    }
                    else
                    {
                        <p class="text-muted">No Keycloak roles available. Make sure Keycloak is running and roles are created.</p>
                    }
                </div>
            </div>

            <div class="form-group">
                <label asp-for="Password" class="form-label"></label>
                <input asp-for="Password" class="form-control" type="password" placeholder="Min 6 characters" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="ConfirmPassword" class="form-label"></label>
                <input asp-for="ConfirmPassword" class="form-control" type="password" placeholder="Re-enter password" />
                <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
            </div>

            <button type="submit" class="btn-submit">Create Account</button>
        </form>

        <div class="auth-footer">
            <p>Already have an account? <a asp-controller="Account" asp-action="Login">Login here</a></p>
        </div>

        <div class="sso-info-box">
            <p class="sso-info-text">
                🔄 <strong>Auto-Sync:</strong> Your account will be automatically created in Keycloak, 
                enabling you to use Single Sign-On across all integrated applications!
            </p>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

